name: Keep Streamlit App Alive

on:
  schedule:
    # 每天國際標準時間 00:00 (台灣時間早上 8:00) 自動執行一次
    - cron: '0 0 * * *'
  workflow_dispatch: # 允許手動觸發測試

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://lrrwnorhkrxze5ujvwas6f.streamlit.app/
